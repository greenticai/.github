name: Nightly Develop

on:
  schedule:
    - cron: '0 2 * * *'  # 2 AM UTC daily
  workflow_dispatch:
    inputs:
      tier:
        description: 'Process a specific tier only (empty = all tiers 0-8)'
        required: false
        type: string
        default: ''
      force:
        description: 'Skip change detection — publish all repos'
        required: false
        type: boolean
        default: false

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  orchestrate:
    name: Tier-ordered dev publish
    runs-on: ubuntu-latest
    outputs:
      summary: ${{ steps.run.outputs.summary }}
      failure_details: ${{ steps.run.outputs.failure_details }}
      failed_repo: ${{ steps.run.outputs.failed_repo }}
      failed_tier: ${{ steps.run.outputs.failed_tier }}
      failed_job_name: ${{ steps.run.outputs.failed_job_name }}
      failed_job_url: ${{ steps.run.outputs.failed_job_url }}
      failure_error: ${{ steps.run.outputs.failure_error }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      # Orchestrate mints (and refreshes) per-org App tokens itself from the
      # App credentials below — runs commonly exceed the 1-hour App-token TTL,
      # so a single upfront create-github-app-token@v2 mint would expire mid-run
      # and stall wait_for_all in 401 polls until MAX_WAIT (3600s) fires.
      - name: Orchestrate dev publishing
        id: run
        env:
          GREENTIC_CI_APP_ID: ${{ secrets.GREENTIC_CI_APP_ID }}
          GREENTIC_CI_APP_PRIVATE_KEY: ${{ secrets.GREENTIC_CI_APP_PRIVATE_KEY }}
          INPUT_TIER: ${{ inputs.tier }}
          INPUT_FORCE: ${{ inputs.force }}
        run: bash scripts/nightly-develop-orchestrate.sh

  cargo-lock-sync:
    name: Cargo.lock sync
    needs: [orchestrate]
    if: needs.orchestrate.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      summary: ${{ steps.sync.outputs.cargo_lock_summary }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - uses: dtolnay/rust-toolchain@1.95.0

      - name: Mint GitHub App token (greenticai)
        id: app-token-greenticai
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.GREENTIC_CI_APP_ID }}
          private-key: ${{ secrets.GREENTIC_CI_APP_PRIVATE_KEY }}
          owner: greenticai

      - name: Mint GitHub App token (greentic-biz)
        id: app-token-greentic-biz
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.GREENTIC_CI_APP_ID }}
          private-key: ${{ secrets.GREENTIC_CI_APP_PRIVATE_KEY }}
          owner: greentic-biz

      - name: Sync Cargo.lock across repos
        id: sync
        env:
          GH_TOKEN_GREENTICAI:   ${{ steps.app-token-greenticai.outputs.token }}
          GH_TOKEN_GREENTIC_BIZ: ${{ steps.app-token-greentic-biz.outputs.token }}
        run: bash scripts/nightly-cargo-lock-sync.sh

  notify-success:
    name: Notify on success
    needs: [orchestrate, cargo-lock-sync]
    if: success()
    uses: ./.github/workflows/notify-success.yml
    with:
      workflow-name: nightly-develop
      details: >-
        ${{ needs.orchestrate.outputs.summary }}${{
          needs.cargo-lock-sync.outputs.summary
            && format(' | lock sync: {0}', needs.cargo-lock-sync.outputs.summary)
            || ''
        }}
    secrets:
      SLACK_WEBHOOK_RELEASE_ALERTS: ${{ secrets.SLACK_WEBHOOK_RELEASE_ALERTS }}

  notify-failure:
    name: Notify on failure
    needs: [orchestrate, cargo-lock-sync]
    if: failure()
    uses: ./.github/workflows/notify-failure.yml
    with:
      workflow-name: nightly-develop
      step: >-
        ${{
          needs.orchestrate.result == 'failure'
            && 'downstream dev-publish'
            || 'cargo-lock-sync'
        }}
      error: >-
        ${{
          needs.orchestrate.result == 'failure'
            && 'Nightly dev-publish orchestration failed. Failed repos:'
            || 'Nightly Cargo.lock sync failed — check the run log'
        }}
      details: ${{ needs.orchestrate.outputs.failure_details }}
    secrets:
      SLACK_WEBHOOK_RELEASE_ALERTS: ${{ secrets.SLACK_WEBHOOK_RELEASE_ALERTS }}
